import React from 'react'

function Title({name,title}) {
    return (
        <div className="title">
            <h2>{name}</h2>
    <h3>{title}</h3>
            
            
        </div>
    )
}

export default Title
